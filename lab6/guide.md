Конечно. Вот подробный гайд для сдачи вашей лабораторной работы по защищенному режиму, включая пошаговую последовательность демонстрации и объяснение ключевых директив ассемблера.

-----

# 🎓 Гайд по сдаче лабораторной работы (Вариант 5)

Ваша задача — не просто показать работающий код, а доказать, что вы понимаете механизмы **защиты памяти** в процессорах x86.

## 1\. Последовательность выступления и демонстрации

Говорите последовательно, объясняя каждый шаг.

### Шаг 1: Введение и Цель (1 минута)

1.  **Тема:** Лабораторная работа №6 — «Защищенный режим микропроцессоров Intel x86».
2.  **Цель работы:** Продемонстрировать переход процессора из Реального режима (DOS) в Защищенный режим и проверить работу **аппаратного механизма защиты** на примере **Особой ситуации 11 (Segment Not Present)**.
3.  **Мой вариант 5:** Требовалось создать дескриптор неприсутствующего сегмента и обработчик исключения 11, который выводит сообщение об ошибке.

-----

### Шаг 2: Теория: Структуры Защищенного Режима (2–3 минуты)

Объясните, что нужно для входа в Защищенный режим:

1.  **GDT (Global Descriptor Table):**

      * Глобальная таблица, хранящая **дескрипторы** (8-байтовые записи, описывающие сегменты памяти).
      * Объясните, что в GDT есть дескрипторы для кода (`Cs_Code`), данных (`Cs_Data`) и видеопамяти (`Video_Desc`).
      * **🔑 Ключевой момент:** Я добавил в GDT дескриптор для проверки ошибки (`Gdt_NP`) и сознательно установил в его байте прав доступа **бит P (Present)** в **0** (`Non_Present_Access Equ 00010011b`). Это делает сегмент "неприсутствующим" для процессора.

2.  **IDT (Interrupt Descriptor Table):**

      * Таблица, хранящая **гейты (Gate)** — указатели на обработчики прерываний и исключений.
      * **🔑 Ключевой момент:** Я изменил 11-й элемент IDT, чтобы он указывал не на заглушку, а на мой обработчик **`ex11_proc`**.

3.  **Переход:** После инициализации GDT и IDT, переход осуществляется установкой бита `PE` (Protection Enable) в управляющем регистре **`CR0`** (`or eax, 1; mov cr0, eax`).

-----

### Шаг 3: Демонстрация Кода и Ошибки (3–5 минут)

Покажите две ключевые части кода:

1.  **Создание ошибки (в `MY_PROC`):**

      * "Рабочая" процедура (`MY_PROC`) сначала очищает экран и выводит строку **"Protected Mode$"**.
      * Далее идет команда-триггер:
        ```asm
        mov ax, Np_Seg ; Загружаем селектор неприсутствующего сегмента
        mov es, ax     ; <--- Здесь процессор генерирует Особую ситуацию 11
        ```
      * **Объяснение:** Эта команда заставляет процессор обратиться к GDT, увидеть `P=0` и **аппаратно** вызвать исключение 11.

2.  **Обработчик ошибки (`ex11_proc`):**

      * Когда возникает исключение 11, процессор переходит по адресу из IDT в мой обработчик `ex11_proc`.
      * Функция `ex11_proc` выводит на экран **ярко-красное** сообщение: **"Exception 11: Segment Not Present\!$"** и останавливает процессор командой `hlt`.

-----

### Шаг 4: Запуск Программы (1 минута)

1.  **Запуск:** `TASM demo.asm`, `Tlink demo.obj /t`, затем `demo`.
2.  **Результат:** Программа запускается, переходит в Защищенный режим, печатает `Protected Mode$` и немедленно **"падает"**, выводя красное сообщение об ошибке.
3.  **Вывод:** Это доказывает, что:
      * Переход в Защищенный режим успешен.
      * Механизм защиты процессора сработал корректно.
      * Мой кастомный обработчик ошибки был вызван и выполнил свою функцию.

-----

-----

## 2\. Справочник директив Turbo Assembler (TASM)

### 1\. Директивы определения данных

| Директива | Назначение | Аналог (Пример) |
| :--- | :--- | :--- |
| **`DB`** (Define Byte) | Определяет байт или строку символов. | `Mess DB 'Protected Mode$'` |
| **`DW`** (Define Word) | Определяет слово (2 байта). | `Len DW 14d` |
| **`DD`** (Define Doubleword) | Определяет двойное слово (4 байта). | `My_Addr DD 0FFFFFFFFh` |
| **`EQU`** (Equate) | Присваивает имя константе или адресу (не занимает места в памяти). | `Cs_Code EQU $-gdt` (Селектор равен текущему смещению относительно `gdt`) |
| **`LABEL`** | Дает метке атрибут типа (например, `WORD`, `BYTE`). | `Gdt LABEL word` |

### 2\. Директивы определения структур и макросов

| Директива | Назначение |
| :--- | :--- |
| **`STRUC...ENDS`** | Определяет шаблон структуры данных (ваши `Gdt_Descriptor` и `Idt_Descriptor`). |
| **`MACRO...ENDM`** | Определяет макрос — именованную последовательность команд, которая вставляется в код при трансляции (ваш `FILLDESCR`). |

### 3\. Директивы сегментации и процедур

| Директива | Назначение |
| :--- | :--- |
| **`.386p`** | Указывает транслятору TASM, что нужно использовать инструкции процессора **80386** и выше, включая Protected Mode (важно для 32-битных команд вроде `shl edx, 4`). |
| **`SEGMENT...ENDS`** | Определяет логический сегмент программы (`CSEG`). |
| **`ASSUME`** | Сообщает транслятору, какой регистр-сегментатор (`CS`, `DS`, `SS`, `ES`) ассоциирован с каким сегментом. |
| **`ORG`** (Origin) | Устанавливает начальный адрес смещения в сегменте (в .COM файлах это всегда `ORG 100h`). |
| **`EVEN`** | Выравнивает следующую инструкцию или данные по границе слова (четный адрес). |
| **`PROC...ENDP`** | Определяет процедуру/функцию. |
| **`END`** | Конец исходного файла, указывает точку начала выполнения (`END Start`). |

### 4\. Директивы и операторы в коде

| Директива | Назначение |
| :--- | :--- |
| **`OFFSET`** | Возвращает смещение переменной/метки в текущем сегменте. |
| **`SHL / SHR`** | Сдвиг влево / вправо (используется для 32-битной арифметики в `Form_32Bit_Address`). |
| **`IMUL`** | Умножение с одним операндом (используется для расчета смещения строки в видеопамяти: `imul dx, 160d`). |
| **`DB 0EAh`** | **Ключевой хак:** Используется для создания **дальнего перехода** (`JMP far`) в коде. Необходимо для смены режима (с Реального на Защищенный), так как прямой переход по новому селектору невозможен. |